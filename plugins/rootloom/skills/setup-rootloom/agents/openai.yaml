interface:
  display_name: "Set Up Rootloom"
  short_description: "Choose and install a Codex capability layer"
  default_prompt: "Use $setup-rootloom to plan and apply the Personal Core preset safely."
policy:
  allow_implicit_invocation: false
