interface:
  display_name: "Operating Code Review"
  short_description: "Review code with evidence-backed findings first"
  default_prompt: "Use $operating-code-review to review this change and report evidence-backed findings without modifying code."
policy:
  allow_implicit_invocation: true
