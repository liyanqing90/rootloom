# Rootloom Personal Core 3.2.0 Publication Record

- Status: published
- Release: `v3.2.0`
- Commit: `9935bbd8f8ab80ae49f9e2e626a5c62e8e4ac51c`
- Annotated tag object: `b2a6bfa043847d9462607a67a84c7ac5f8f96611`
- Pull request: <https://github.com/liyanqing90/rootloom/pull/9>
- Release URL: <https://github.com/liyanqing90/rootloom/releases/tag/v3.2.0>
- GitHub Release ID: `RE_kwDOTVQo6M4VIbQT`
- GitHub Release database ID: `354530323`
- Published at: 2026-07-15T15:20:01Z
- Draft: false
- Prerelease: false

## Verification

- The Index-suppression counterexample failed three assertions before the correction: Intake accepted both `assume-unchanged` and `skip-worktree`, and Finalizer accepted a flag added after Intake. The same end-to-end regression passed after bounded `git ls-files -v -z` checks were added at the shared capture boundary.
- Local `make check` passed repository validation and 181 tests with two environment-only skips because the current macOS volume cannot create case-distinct paths or a non-UTF-8 filename. `make compatibility-smoke` passed with Codex CLI 0.144.2, and `git diff --check` passed.
- A clean clone of candidate `e98b0871ac33f5533c3e39ebf97bc5107dccff03` independently passed the same 181-test gate and compatibility smoke with no managed rollback leftovers.
- Release PR run [29426683030](https://github.com/liyanqing90/rootloom/actions/runs/29426683030) passed:
  - `Linux Python 3.11`
  - `Linux Python 3.12`
  - `Linux Python 3.13`
  - `Linux Python 3.14`
  - `Portable contracts (macos-latest)`
  - `Portable contracts (windows-latest)`
  - `Codex CLI contracts`
- The exact merged `main` run [29427110772](https://github.com/liyanqing90/rootloom/actions/runs/29427110772) independently passed the same seven jobs before tagging.
- Publication-record CI is pending for the commit that introduces this record and its validator contract.
- Annotated tag `v3.2.0` peels to the passing release commit. Its tree is identical to the PR candidate tree, and remote `main` resolved to that commit at publication time.
- The GitHub Release is the latest public Release, non-draft, and non-prerelease.
- The unrelated untracked `assets/rootloom-xiaohei-loom.png` remains outside every commit and release artifact with SHA-256 `55ea78b17ac40b85cf347b478799dbda629ba8f4d52e7cea5044734c6ec1c0a6`.
- The separate `codex/enterprise-assurance` branch remains unchanged at `7da82dc8ad0faee0aa6a51c569bab8a60c233b8a`.

## Notes

This Minor release closes the reviewable-path capture and privacy boundary. Ignored, Index-suppressed, hardlinked, and case-ambiguous declarations fail closed at Intake and during stable capture; DER is ambiguous material rather than a certificate-specific public format; common PEM/DER key names remain non-overridable; and Summary revision 5 now discloses the exact sealed reviewability policy and final file identity metadata. Default baseline v3, the published baseline-v4 schema, and baseline v2/v3/v4 read/seal compatibility remain unchanged.
