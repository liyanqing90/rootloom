# Rootloom Personal Core 3.0.0 Publication Record

- Status: published
- Release: `v3.0.0`
- Commit: `61c1d8202350442721644345678e1525461c7d08`
- Annotated tag object: `72091416819a5b16436f2be0b7862dc965856f49`
- Pull request: <https://github.com/liyanqing90/rootloom/pull/7>
- Release URL: <https://github.com/liyanqing90/rootloom/releases/tag/v3.0.0>
- GitHub Release ID: `RE_kwDOTVQo6M4VHOPs`
- Published at: 2026-07-15T05:07:33Z
- Draft: false
- Prerelease: false

## Verification

- Local `make check` passed repository validation and 164 tests with one environment-only skip because the current macOS volume rejects a non-UTF-8 filename; `make compatibility-smoke`, `make validate`, and `git diff --check` passed.
- A clean temporary-clone governed replay returned summary revision 5, `REVIEW_EVIDENCE_COMPLETE`, `evidence_complete: true`, `passed: true`, `change_partition: exact`, complete claim binding, and a valid hash chain for the 27 intended release paths. Its measured capture duration was 0.325201 seconds.
- Release PR run [29390214510](https://github.com/liyanqing90/rootloom/actions/runs/29390214510) passed:
  - `Linux Python 3.11`
  - `Linux Python 3.12`
  - `Linux Python 3.13`
  - `Linux Python 3.14`
  - `Portable contracts (macos-latest)`
  - `Portable contracts (windows-latest)`
  - `Codex CLI contracts`
- The merged `main` run [29390437387](https://github.com/liyanqing90/rootloom/actions/runs/29390437387) independently passed the same seven jobs before tagging.
- Publication-record run [29390839339](https://github.com/liyanqing90/rootloom/actions/runs/29390839339) passed the same seven jobs after the release facts and validator contract were added to `main`.
- Annotated tag `v3.0.0` peels to the passing release commit, and remote `main` resolved to that commit at publication time.
- The GitHub Release is public, non-draft, and non-prerelease.
- Local Codex installed and enabled `rootloom@rootloom` at 3.0.0. Because the repository marketplace is local rather than Git-backed, `marketplace upgrade` correctly declined; `plugin add` refreshed the plugin cache from 2.4.0 to 3.0.0. The managed personal preset upgraded from 2.4.0 to 3.0.0 and reports no drift or pending upgrade.
- Installed Rules return `allow` for representative routine and high-risk argv while catastrophic `rm -rf /` remains `forbidden`; authorization-mode semantics remain owned by global guidance.
- The unrelated untracked `assets/rootloom-xiaohei-loom.png` remains outside every commit and release artifact.
- The separate `codex/enterprise-assurance` branch remains unchanged at `7da82dc8ad0faee0aa6a51c569bab8a60c233b8a`.

## Notes

This Major release separates secret-material privacy quarantine from security-domain risk, detects common CamelCase credential material, adds one monotonic deadline per stable capture lifecycle, advances summaries to revision 5 with stable `evidence_complete`, produces baseline v3, and replaces identity-suggesting provenance names. Historical revision-4 summaries remain immutable and valid baseline v2 evidence remains readable and sealable. Path-only secret inference and a single non-interruptible filesystem metadata syscall remain documented residual boundaries rather than correctness claims.
