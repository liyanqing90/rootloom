# Rootloom Personal Core 2.4.0 Publication Record

- Status: published
- Release: `v2.4.0`
- Commit: `e434654958109ef6f6a3878ef8a3db36226cec54`
- Annotated tag object: `df0bb82d8253724b362988a8540e099e903df627`
- Pull request: <https://github.com/liyanqing90/rootloom/pull/6>
- Release URL: <https://github.com/liyanqing90/rootloom/releases/tag/v2.4.0>
- GitHub Release ID: `RE_kwDOTVQo6M4VHEQf`
- Published at: 2026-07-15T02:36:50Z
- Draft: false
- Prerelease: false

## Verification

- Local `make check` passed repository validation and 156 tests with one environment-only skip because the current macOS volume rejects a non-UTF-8 filename; `make compatibility-smoke` returned `passed: true` with no failed commands, managed leftovers, or plugin-install side effects.
- The governed release finalizer returned `REVIEW_EVIDENCE_COMPLETE`, `passed: true`, `change_partition: exact`, complete claim binding, and a valid hash chain. The unrelated image remained only in pre-existing evidence and outside task patch headers.
- The first PR run [29382878992](https://github.com/liyanqing90/rootloom/actions/runs/29382878992) exposed a Windows Job Object accounting race and was intentionally not retried or bypassed. The owning-boundary repair added a bounded post-exit convergence grace and regression coverage.
- The replacement release PR run [29383392662](https://github.com/liyanqing90/rootloom/actions/runs/29383392662) passed:
  - `Linux Python 3.11`
  - `Linux Python 3.12`
  - `Linux Python 3.13`
  - `Linux Python 3.14`
  - `Portable contracts (macos-latest)`
  - `Portable contracts (windows-latest)`
  - `Codex CLI contracts`
- The merged `main` run [29383564033](https://github.com/liyanqing90/rootloom/actions/runs/29383564033) independently passed the same seven jobs before tagging.
- Annotated tag `v2.4.0` peels to the passing release commit, and remote `main` resolved to that commit at publication time.
- The GitHub Release is public, non-draft, and non-prerelease.
- Local Codex installed and enabled `rootloom@rootloom` at 2.4.0. Because the repository marketplace is local rather than Git-backed, `marketplace upgrade` correctly declined; `plugin add` refreshed the plugin cache to 2.4.0. The managed personal preset upgraded from 2.3.0 to 2.4.0 and reports no drift or pending upgrade.
- Installed Rules return `allow` for representative routine and high-risk argv while catastrophic `rm -rf /` remains `forbidden`; authorization-mode semantics remain owned by global guidance.
- The separate `codex/enterprise-assurance` branch remains unchanged at `7da82dc8ad0faee0aa6a51c569bab8a60c233b8a`.

## Notes

This release makes Strict Review evidence-honest and bounded, fixes exact dirty-baseline attribution across analysis and bundle construction, closes Git stdin deterministically, separates sensitive candidate/result budgets, and verifies Windows process-tree convergence. The unrelated untracked `assets/rootloom-xiaohei-loom.png` file was excluded and preserved at SHA-256 `55ea78b17ac40b85cf347b478799dbda629ba8f4d52e7cea5044734c6ec1c0a6`.
